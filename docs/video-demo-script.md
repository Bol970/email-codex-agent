# Video Demo Script

Сценарий записи учебного проекта Email Codex Agent.

Цель: примерно за 4 минуты показать локальный email workspace с AgentMail и встроенным Codex, который помогает читать письма, суммировать их и готовить черновики без автоматической отправки.

## Перед записью

1. Закрыть лишние вкладки, `.env.local`, терминалы с секретами и уведомления.
2. Открыть запись экрана на отдельном мониторе.
3. Запустить автономный демо-режим:

```bash
npm run demo:record
```

Команда сама:

- соберёт production build;
- поднимет безопасный mock+demo сервер;
- откроет отдельное окно Chromium;
- постарается открыть окно на мониторе `2`;
- покажет экранные титры;
- проиграет мягкие звуковые эффекты для титров, курсора, кликов и финала;
- будет плавно двигать видимый demo-курсор к целевым кнопкам;
- откроет письмо;
- нажмёт `Summarize`;
- нажмёт `Draft reply`;
- покажет созданный черновик.
- покажет финальный титр и автоматически закроет browser.

## Роли

Пользователь:

- включает запись;
- ждёт, пока demo-runner сам пройдёт сценарий;
- после закрытия browser останавливает запись.

Codex:

- сам открывает браузер и кликает UI через Playwright;
- ведёт видимый курсор по экрану перед каждым кликом;
- сопровождает ключевые шаги короткими звуковыми эффектами;
- показывает экранные титры вместо устной озвучки;
- не отправляет письма.

## Таймлайн на 4 минуты

### 0:00 - 0:40. Что это за проект

Что показать: общий вид приложения.

Текст:

> Это локальный учебный сервис Email Codex Agent. Он работает как веб-приложение на localhost, подключается к AgentMail inbox и добавляет справа встроенную панель Codex для работы с письмами.

Показать:

- левую панель с inbox и фильтрами;
- список писем;
- область чтения;
- правую панель Codex.

### 0:40 - 1:25. Устройство интерфейса

Что показать:

1. Левую панель inbox и фильтров.
2. Список писем.
3. Правую панель Codex.

Текст:

> Интерфейс устроен как рабочий почтовый клиент: слева inbox и фильтры, в центре список писем, справа агент Codex. В demo-режиме используются безопасные mock-письма.

### 1:25 - 2:00. Чтение письма

Что сделать:

1. Открыть одно входящее письмо.
2. На секунду показать тему, текст письма и область ответа.

Текст:

> Базовый workflow такой же, как в обычном почтовом клиенте: я открываю входящее письмо, читаю его содержимое, вижу thread и могу вручную ответить или сохранить черновик.

### 2:00 - 2:50. Codex summary

Что сделать:

1. Нажать `Summarize` в правой панели.
2. Дождаться ответа в Codex-чате.

Текст для Summarize:

> Codex получает выбранный email thread как контекст и помогает быстро понять смысл письма: кто написал, что нужно сделать, есть ли срочность и какой следующий шаг лучше выбрать.

### 2:50 - 3:35. Черновик ответа

Что сделать:

1. Нажать `Draft reply`.
2. Дождаться появления сообщения о создании черновика.
3. Показать карточку draft под письмом.
4. Не нажимать `Send`.

Текст:

> Главная идея безопасности здесь - draft-first. Codex может подготовить ответ, но не отправляет письмо сам. Отправка остаётся только за пользователем через явный клик.

Показать:

- сообщение `Черновик создан в выбранном письме`;
- текст черновика;
- кнопки `Send` и удаления draft.

### 3:35 - 4:10. Ручной контроль

Что сделать:

1. Показать поле `Write a reply`.
2. Не обязательно вводить текст; достаточно показать, что ручной ответ тоже доступен.

Текст:

> При этом приложение не заменяет пользователя. Можно самому написать ответ, сохранить draft и решить, отправлять его или нет. Codex здесь работает как помощник внутри привычного mail workflow.

### 4:10 - 4:25. Финал

Что показать: финальный титр и автоматическое закрытие browser.

Текст:

> В итоге это локальный email workspace: AgentMail отвечает за почту, Codex помогает с анализом и черновиками, а все необратимые действия остаются под контролем пользователя.

## Автопилот во время записи

`npm run demo:record` делает всё сам:

1. Открывает demo browser.
2. Показывает титр с назначением проекта.
3. Плавно ведёт demo-курсор к письму `Pilot inbox: launch checklist`.
4. Открывает письмо.
5. Ведёт курсор к `Summarize` и запускает summary.
6. Показывает ответ Codex.
7. Ведёт курсор к `Draft reply` и создаёт черновик.
8. Показывает созданный draft.
9. Показывает финальный титр.
10. Закрывает browser и останавливает demo-сервер.

После автоматического закрытия browser можно останавливать запись. Если нужно оставить окно открытым для ручной проверки, запускать так: `DEMO_KEEP_OPEN=1 npm run demo:record`.
Если звук не нужен, запускать так: `DEMO_SOUND=0 npm run demo:record`.

## Что не показывать и не проговаривать

- Не рассказывать про настройки приватности.
- Не открывать `.env.local`.
- Не показывать ключи, терминалы с секретами или историю команд.
- Не нажимать `Send`, если это не отдельный тестовый inbox.
- Не уходить в GitHub, README или код во время записи.

## Проверка перед записью

Запустить `npm run demo:record` один раз без записи и проверить, что браузер сам дошёл до финального титра и закрылся.
